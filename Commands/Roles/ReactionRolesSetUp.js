const { CommandInteraction, MessageEmbed } = require('discord.js')
const logger = require('../../Systems/Logs').Logger;

module.exports = {
    name: "reactionrole",
    description: "Reaction role set up",
    permission: "ADMINISTRATOR",
    /**
     * 
     * @param {CommandInteraction} interaction
     */ 
    
}